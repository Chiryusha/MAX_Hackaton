# Чат-бот ВСЕЗНАЙКА 🤖

Чат-бот для помощи студентам в мессенджере MAX с функциями регистрации на мероприятия и умными напоминаниями.

## 🎯 Основные функции

- ✅ **Мгновенная регистрация** - регистрация без сторонних платформ
- 🔔 **Умные напоминания о мероприятиях** - автоматические уведомления о событиях
- 📅 **Управление мероприятиями** - просмотр, подписка и отписка от событий

## 📋 Требования

- Python 3.9+
- Docker (для запуска через Docker)
- Токен бота MAX мессенджера

## 🚀 Быстрый старт

### Вариант 1: Запуск через Docker (Рекомендуется)

1. **Клонируйте репозиторий или скопируйте файлы проекта**

2. **Создайте директорию для данных и установите права доступа:**
```bash
mkdir -p data
chmod 755 data
```

> **Важно:** Директория `data` будет монтироваться в контейнер, поэтому убедитесь, что у пользователя, от имени которого запущен Docker, есть права на запись в эту директорию.

3. **Соберите Docker-образ:**
```bash
docker build -t vseznayka-bot .
```

4. **Запустите контейнер:**
```bash
docker-compose up -d
```

Или без docker-compose:
```bash
docker run -d --name vseznayka_bot --restart unless-stopped vseznayka-bot
```

5. **Проверьте логи:**
```bash
docker logs -f vseznayka_bot
```

### Вариант 2: Локальный запуск

1. **Создайте виртуальное окружение:**
```bash
python -m venv myenv
```

2. **Активируйте виртуальное окружение:**
   - Windows:
   ```bash
   myenv\Scripts\activate
   ```
   - macOS/Linux:
   ```bash
   source myenv/bin/activate
   ```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте токен бота:**
   Откройте файл `bot.py` и замените значение `TOKEN` на ваш токен API бота MAX.

5. **Запустите бота:**
```bash
python bot.py
```

## 📁 Структура проекта

```
.
├── bot.py                 # Основной файл бота
├── database.py            # Модуль работы с базой данных
├── handlers.py            # Обработчик команд
├── notifications.py       # Сервис напоминаний
├── requirements.txt       # Зависимости Python
├── Dockerfile            # Конфигурация Docker
├── docker-compose.yml    # Docker Compose конфигурация
├── .dockerignore         # Исключения для Docker
└── README.md            # Документация
```

## 🎮 Команды бота

### Личный кабинет:
- `/start` - Регистрация/приветствие
   `/register` - Создание личного кабинета

### Мероприятия:
- `/events` - Список всех мероприятий
- `/my_events` - Мои мероприятия
- `/subscribe <ID>` - Подписаться на мероприятие
- `/unsubscribe <ID>` - Отписаться от мероприятия

### Помощь:
- `/help` - Список всех команд

## 🔧 Настройка

### База данных

Бот использует JSON-файл `database.json` для хранения данных. Файл создается автоматически при первом запуске.

### Напоминания

Сервис напоминаний автоматически проверяет мероприятия и отправляет уведомления:
- За 1 день до мероприятия
- За 1 час до мероприятия
- За 15 минут до мероприятия

Проверка происходит каждые 5 минут.

## 🐳 Docker

### Сборка образа:
```bash
docker build -t vseznayka-bot .
```

### Запуск контейнера:
```bash
docker-compose up -d
```

### Остановка:
```bash
docker-compose down
```

### Просмотр логов:
```bash
docker logs -f vseznayka_bot
```

## 📝 Добавление тестовых мероприятий

Для тестирования можно добавить мероприятия вручную в файл `database.json` или создать скрипт для добавления. Формат мероприятия:

```json
{
  "id": 0,
  "title": "Название мероприятия",
  "description": "Описание",
  "date": "2024-12-31T12:00:00",
  "organizer": "Организатор",
  "created_at": "2024-01-01T00:00:00",
  "subscribers": []
}
```

## 🔒 Безопасность

- Токен бота хранится в коде (для продакшена рекомендуется использовать переменные окружения)
- База данных хранится локально в JSON-файле
- Все ошибки логируются

## 📊 Логирование

Бот ведет логи всех операций. Логи выводятся в консоль с уровнем INFO.

## 🛠 Разработка

### Добавление новых команд

1. Откройте `bot.py`
2. Добавьте обработчик в функцию `handle_message()`
3. Используйте декоратор `@bot.on_message()` для обработки сообщений

### Расширение функциональности

- `database.py` - работа с данными
- `notifications.py` - система напоминаний
- `bot.py` - основная логика бота

## 📄 Лицензия

Проект создан для хакатона MAX.

## 🔧 Устранение проблем

### Проблема: Permission denied для database.json

Если при разворачивании через Docker появляется ошибка `Permission denied` для файла `database.json`:

1. **Проверьте, что директория `data` существует на хосте:**
```bash
mkdir -p data
chmod 755 data
```

2. **Убедитесь, что у директории правильные права доступа:**
```bash
# На Linux/macOS
sudo chown -R $USER:$USER data
chmod 755 data

# Или установите права для всех (менее безопасно, но для тестирования подойдет)
chmod 777 data
```

3. **Если директория создается внутри контейнера, проверьте права:**
```bash
docker exec -it vseznayka_bot ls -la /app/data
docker exec -it vseznayka_bot touch /app/data/test.txt
```

4. **Пересоберите образ Docker:**
```bash
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

5. **Проверьте логи на наличие ошибок:**
```bash
docker logs vseznayka_bot
```

### Другие проблемы

**Бот не отвечает:**
- Проверьте правильность токена в `bot.py`
- Убедитесь, что контейнер запущен: `docker ps`
- Проверьте логи: `docker logs vseznayka_bot`

**Ошибки импорта:**
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
- Пересоберите Docker-образ: `docker-compose build --no-cache`

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота
2. Наличие всех зависимостей
3. Логи бота на наличие ошибок
4. Права доступа к директории `data` (если используете Docker)

---

**Чат-бот ВСЕЗНАЙКА** - ваш помощник в организации студенческой жизни! 🎓
